name: C++ CI/CD Pipeline

on:
  push:
    branches:
      - main
      - dev
      - CD/CI
  pull_request:
    branches:
      - main
      - dev
      - CD/CI
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the code
    - name: Checkout code
      uses: actions/checkout@v3

    # Step 2: Install dependencies
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake make g++ wget unzip

    # Step 3: Fetch and install Google Test
    - name: Fetch Google Test
      run: |
        cd quickest_path
        mkdir -p googletest
        cd googletest
        wget https://github.com/google/googletest/archive/refs/tags/v1.15.2.zip
        unzip v1.15.2.zip
        cd googletest-1.15.2
        cmake .
        make
        sudo make install # Installs Google Test globally

    # Step 4: Build the project
    - name: Build the project
      run: |
        cd quickest_path
        mkdir build
        cd build
        cmake ..
        make

    # Step 5: Run tests
    - name: Run Tests
      run: |
        ctest --output-on-failure
