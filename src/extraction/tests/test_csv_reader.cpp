// // ‿︵‿୨ Linking ୧‿︵‿

/*
#include <gtest/gtest.h>
#include "csv_reader.hpp"

TEST(CSVReaderTest, LoadsValidData) {
    auto roads = readCSV("path/to/testfile.csv");
    ASSERT_FALSE(roads.empty());
    EXPECT_EQ(roads[0].landmarkA, 1);
    EXPECT_EQ(roads[0].landmarkB, 2);
    EXPECT_EQ(roads[0].time, 10);
}
*/